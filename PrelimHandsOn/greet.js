var greetings = {
  greet: function (myname) {
    return 'Hello, ' + myname;
  },

  congratulations: function (myname) {
    return 'Congratulations, ' + myname;
  },

  farewell: function (myname) {
    return 'Goodbye, ' + myname;
  },
};
module.exports = greetings;
